{
  "name": "@hawaii-bus-plus/utils",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --build src",
    "format": "prettier **/*.ts --write",
    "pretest": "tsc --build test",
    "test": "ava"
  },
  "dependencies": {
    "@hawaii-bus-plus/types": "*",
    "tslib": "^2.1.0"
  },
  "devDependencies": {
    "prettier": "^2.2.1",
    "typescript": "4.1.3"
  }
}
