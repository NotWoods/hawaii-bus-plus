{
  "name": "@hawaii-bus-plus/identity-email",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "copyfiles templates/*.html ../../dist/email/ --flat",
    "format": "prettier **/*.html --write --ignore-path ../../.prettierignore"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "prettier": "~3.6.2"
  }
}
