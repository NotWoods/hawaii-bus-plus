import type { Meta, StoryObj } from '@storybook/preact';
import { LoadingBusIcon } from './LoadingBusIcon';

const meta = {
  component: LoadingBusIcon,
} satisfies Meta;

export default meta;
type Story = StoryObj;

export const Icon: Story = {};
